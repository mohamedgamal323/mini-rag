services:
  mongodb:
    image: mongo:latest
    
    container_name: mongodb
    
    ports:
      - "27007:27017"
    
    volumes:
      - mongodb_vol_data:/data/db
    
    networks:
      - backend
    
    restart: always

networks:
  backend:

volumes:
      mongodb_vol_data: 
